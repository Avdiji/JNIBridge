            ${cType} ${cVar};
            if(${jniVar}) {
                const char* c_str${id} = env->GetStringUTFChars(${jniVar}, nullptr);
                ${cVar} = ${cType}(c_str${id});
                env->ReleaseStringUTFChars(${jniVar}, c_str${id});
            }